import React from "react";
import "./LoaderSpinner.css"; // Custom CSS file

const LoaderSpinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default LoaderSpinner;
